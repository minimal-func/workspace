<!DOCTYPE html>
<html>
  <head>
    <title>MyDay.<%= @app_title %></title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="initial-scale=1, maximum-scale=1">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_link_tag 'included', media: 'all' %>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:200,300,400,400i,500,600,700%7CMerriweather:300,300i" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <% flash.each do |key, value| %>
      <div class="notification pos-right pos-top col-md-4 col-lg-3" data-animation="from-top" data-notification-link="search-box">
        <div class="notification-close-cross"></div>
        <div class="alert alert-<%= key %> mb-0">
          <%= value %>
        </div>
      </div>
    <% end %>
    <div class="main-container">
      <%= render "header" %>
      <div class="container">
        <%= yield %>
      </div>
    </div>
    <%= render "footer" %>
  </body>
</html>
