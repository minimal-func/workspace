<span class="auth-shell__eyebrow">Account Recovery</span>
<h2>Forgot your password?</h2>
<p class="lead">
  Enter your email and we’ll send reset instructions.
</p>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>
  <div class="row">
    <div class="col-md-12">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Your email" %>
    </div>
    <div class="col-md-12">
      <%= f.submit "Send reset instructions", class: "btn btn--primary type--uppercase auth-shell__submit" %>
    </div>
  </div>
<% end %>

<div class="auth-shell__links">
  <%= render "users/shared/links" %>
</div>
